# Respo project reference

Use the installed Respo module documentation for API details:

```bash
calcit libs readme respo.calcit --file docs/Respo-Agent.md --full
calcit docs read upgrade --full
```

Keep this application-specific guide limited to its actual entry points and build commands. The source snapshot is `calcit.cirru`; use `calcit edit` / `calcit tree` for changes and validate with the commands in `Agents.md`.

