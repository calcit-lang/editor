# 2026-08-11 19:40 editor writer picker type cleanup

- Replaced the remaining required `picker-mode` field access in `pick-node` with explicit `get` and Option unwrapping.
- Reduced preprocessing warnings from 9 to 8.
- Built-in `cr test --summary-only --format json` selected and executed 0 tests in this project.
