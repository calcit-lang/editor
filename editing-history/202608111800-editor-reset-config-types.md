# 2026-08-11 18:00 editor reset/config types

- Added explicit writer stack/pointer boundaries in `draft-expr`.
- Unwrapped the configuration map in `file-config` before merging.
- Normalized saved-files, old-file, namespace, and definition restoration values in `reset-at` and `reset-files`.
- Reduced default-entry preprocessing warnings from 106 to 95.
- Verified the built-in Calcit test command; this snapshot currently has 0 selected tests and 0 executed tests.
